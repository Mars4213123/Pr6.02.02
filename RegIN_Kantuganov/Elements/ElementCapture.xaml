<UserControl x:Class="RegIN_Kantuganov.Elements.ElementCapture"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="280"
             d:DesignHeight="100">
    <Grid>
        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="4"
                Width="280" Height="50" VerticalAlignment="Top"
                Background="LightYellow">
            <Grid x:Name="Capture"/>
        </Border>

        <Label Content="Enter 4-digit code below:"
               Margin="0,55,0,0" VerticalAlignment="Top"
               FontSize="11" Foreground="Gray"/>

        <Border BorderBrush="#FF673AB7" BorderThickness="1" CornerRadius="4"
                Margin="0,75,0,0" VerticalAlignment="Top" Height="28">
            <TextBox x:Name="InputCapture"
                     BorderThickness="0"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     Background="Transparent"
                     KeyUp="EnterCapture"/>
        </Border>
    </Grid>
</UserControl>
